# IMPORTANT: For DigitalOcean App Platform Deployment
# Set these as App-Level Environment Variables (encrypted secrets) in your DigitalOcean dashboard:
# App Settings → Environment Variables → Add as "Encrypted" secrets

# ===== REQUIRED ENVIRONMENT VARIABLES =====

# Facebook Configuration (REQUIRED - set as app-level secrets in DigitalOcean)
PAGE_ACCESS_TOKEN=your_page_access_token_here
VERIFY_TOKEN=your_verify_token_here
APP_SECRET=your_app_secret_here

# Gemini API (REQUIRED - set as app-level secret in DigitalOcean)
GEMINI_API_KEY=your_gemini_api_key_here

# Admin Interface (REQUIRED - set as app-level secret in DigitalOcean)
JWT_SECRET=your_jwt_secret_here

# ===== AUTOMATIC/OPTIONAL VARIABLES =====

# Database Configuration
# DATABASE_URL is automatically injected by DigitalOcean when using managed PostgreSQL
# For local development, you can use SQLite:
DB_PATH=./database/bot.db

# Server Configuration (these are set in app.yaml, don't add as env vars)
# PORT=3000 (set in app.yaml)
# NODE_ENV=production (set in app.yaml)

# ===== NOTES =====
# 1. All secrets (tokens, keys) must be set as App-Level Environment Variables in DigitalOcean
# 2. Do NOT commit actual values to git - this file is for documentation only
# 3. DATABASE_URL is automatically provided by DigitalOcean for managed databases
# 4. For local development, create a .env file with your actual values